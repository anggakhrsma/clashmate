services:
  clashmate:
    build: .
    restart: unless-stopped
    env_file: .env
    ports:
      - "8070:8070"
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
